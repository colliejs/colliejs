import { styled } from "../styled";
import { MyButton } from "./MyButton";
export const MySpecialButton = styled(MyButton, {
  background: "pink",
  fontSize: 30,
  borderRadius: 10,
  color:'red'
});
