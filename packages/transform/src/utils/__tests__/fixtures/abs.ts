import { FIX_NUMBER } from "./base";

export const abs = (x: string) => ({ position: x });
export const flex = { display: 'flex' };
export const big = '100px'
export const small = FIX_NUMBER
export const pos = (x: string) => ({ position: x });